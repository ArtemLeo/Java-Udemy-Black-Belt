package Java_30_Nested_classes;

public class Main {
    // Static class;
    static class A {
    }

    // Inner class;
    class B {
    }

    // Local class (класс внутри блока/метода);
    void method() {
        class C {
        }
    }

    // Anonymous class (класс без имени);
}
